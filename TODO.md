# TODO

作成: 2026-08-02（コミット `82aaa65` 時点）

A〜U はすべて決着した（N・O・S・U-1 は「対応しない」、P と Q は方針・文言を
決めて決着）。**残っている項目は無い。**

---

## 完了済み

### U. ロールブックのビューアの操作性

帯（ミニマップ）のドラッグ（U-2）だけ実施。**ホイールとピンチ（U-1）は
対応しない**ことにした。

いまの操作（`storgan.js` の後半）:

| 操作 | 挙動 |
|---|---|
| ホイール | **スクロール**（縦横）。拡縮は Ctrl / ⌘ を押しながら |
| トラックパッドのピンチ | 効く（ブラウザが Ctrl + ホイールとして送るため） |
| スマホのピンチ | 効かない（touch イベントで、ホイールにはならない） |
| SVG 上のドラッグ | パン（`pointerdown` / `pointermove`） |
| 帯（ミニマップ） | クリックでその位置へ。**ドラッグで送れる** |

#### U-1. ホイールとピンチでの拡大縮小（対応しない）

**素のホイールを拡縮に使うと、いまのスクロールを自分で捨てることになる。**
このビューアは transform を使わず SVG の描画サイズそのものを変えていて、
**ブラウザ標準のスクロールがそのまま効くこと**が設計の要になっている
（CLAUDE.md「ロールブックのビューア」参照）。素のホイールを
`preventDefault()` すると、その利点が失われる。

拡縮の手段は既に足りている（Ctrl / ⌘ + ホイール、トラックパッドのピンチ、
倍率スライダ、高さ合わせ / 全体 / 原寸のボタン）ので、**このままとする**。
スマホのピンチも同じ理由で追わない。

#### U-2. 帯（ミニマップ）のドラッグ

`click` をやめ、`pointerdown` → `pointermove` → `pointerup` にした。
`setPointerCapture()` で帯の外へ出ても追従する（SVG 上のパンと同じ作り）。

**掴んだ場所で分けた。**

| 押した場所 | 動き |
|---|---|
| 枠（`.minimap__window` = いま見えている範囲）| 飛ばさない。掴んだ点を保ったまま**相対移動** |
| それ以外 | 押した点が中央に来るよう**その位置へ飛ぶ**（従来のクリックと同じ）。以後は追従 |

枠を掴んだときに飛ばすと、少しだけずらしたいときに中央へ跳ねて扱いにくい。
スクロールバーの摘まみと同じ感覚に合わせた。

指での操作のために `.minimap` に **`touch-action: none`** を付けてある
（既定のままだとページの縦スクロールに取られる）。`pointerdown` の
`preventDefault()` だけでは足りない。

`.minimap__edge`（START / END の字）は `pointer-events: none` なので
そのまま素通りする。

ブラウザテストを 2 本足した。**相対移動の確認は離す前に見ること**
（離してからだと、飛ばすだけの実装でも同じ位置に着いてしまい素通りする）。

### R. HTTP テストが実物の `webroot/` を触っていた

`tests/test_webapp_async.py` が `webroot=Path('./webroot')` と実物を渡していて、
アップロードのテストが `webroot/midi/dummy.mid` と
`webroot/svg/dummy.mid.svg` を**実際に書いていた**。`tearDown` で消しては
いたが、途中で落ちると消し残る。一覧を読むテストは、そこに置いてある
実ファイルの影響も受けていた。

K で足した `tests/test_history.py` は一時ディレクトリに複製する形にして
いたので、**同じ目的のテストが 2 通りのやり方で書かれていた**。

`tests/webapp_base.py` に `WebAppTestCase` を切り出し、両方をそこに載せた。

- `webroot` をテストごとに一時ディレクトリへ複製し、後片付けは
  `addCleanup` に任せる（`tearDown` は不要になった）
- 置き場に何か置きたいときは `setup_files()` を上書きする
  （履歴のテストが MIDI と古い形の SVG を置いている）
- `PORT` と `SERVER_KWARGS`（`debug` / `size_limit`）は subclass が決める

**わざと落としても実物が汚れないことを確認した**（アップロード直後に
例外を投げるテストを一時的に作り、`webroot/` に何も残らないことを見た）。

### Q. 同名アップロードの選択肢（文言を直して決着）

「前回の結果を表示」という名前が実際の動きと合っていなかった。
**保存済みの SVG を出しているのではなく、保存済みの MIDI を変換し直している。**
動き自体はそれでよいので、名前のほうを直した。

    demo2.mid は既にアップロードされています。どちらのファイルで変換しますか？
    [キャンセル] [前回のファイルで変換] [置き換えて変換]

どちらのボタンも**変換はする**。違うのは使うファイルだけ、と読めるように
ラベルを揃えた（履歴の「表示」は本当に変換しないので、そちらとは別物）。

**選んだファイルの中身を送ってから捨てている点は、そのままとする。**
ファイル名を別のフィールドで送れば省けるが、そこからパスを組み立てることに
なるため。副作用として、**上限を超える大きさのファイルではこのダイアログに
たどり着けない**（先に上限で止まる）が、上限は 100 MB なので実害は無い。

### T-2. 図から求まらない値を SVG に埋める

`RollBook._meta_attrs()` が `<svg>` に属性として埋め、
`storage.book_from_svg()` が読む。これで履歴の「表示」でも諸元が全部出る。

    <svg … viewBox="…"
         data-storgan-model="34notes"
         data-storgan-mm-per-sec="50"
         data-storgan-notes="1033"
         data-storgan-hole-notes="562"
         data-storgan-off-scale-notes="471">

埋めるのは**図からは求まらないものだけ**。寸法は `width` / `height` 属性、
穴と破線の数は描かれているものを数えれば分かるので、二重に持たない
（持つと手で編集したときに食い違う）。

| 表示 | 埋める前 | 埋めたあと |
|---|---|---|
| 演奏時間 | `---` | `1:22` |
| 音符 | `---` | `1033` |
| 穴 | `--- → 566 個` | `562 → 566 個` |
| 破線 | `--- → 471 個` | `471 → 471 個` |
| 1 秒 | `---` | `50 mm` |

**属性が無い SVG（埋める前に作ったもの）は `---` のまま**。実際に手元の
古い SVG で確かめた。壊れた値（`data-storgan-notes="abc"`）も `---` に落とす。

名前を `data-storgan-` で始めているのは、他のツールが付けた属性と
紛れないようにするため。`model` は今のところ表示していないが、
どの機種で作ったかは SVG からは分からないので入れてある。

生成 → 読み直しで**全項目が一致する**往復テストがある。色や属性名を
変えると落ちる。

### T-1. 保存済み SVG から穴の数を読む

「穴の数は SVG から取り出せない」と書いていたが、**それは `hole_count` が
「音符の数」だった頃の話**。`hole_count` を「ブリッジで分割したあとの数」に
変えた時点で、描かれている `<path>` を数えれば求まるようになっていたのに、
説明を直さないまま放置していた。

`storage.book_from_svg()` が線の色で数えるようにした。

| | 34notes | 20notes a |
|---|---|---|
| 赤（穴） | 566 = `hole_count` | 967 = `hole_count` |
| 黒破線 | 471 = `off_scale_count` | 3681 = `off_scale_count` |

履歴の「表示」はこう出るようになった（`---` は分割前の音符の数）。

    全長 4133.2 mm  演奏時間 ---  高さ 126.0 mm
    音符 ---  穴 --- → 566 個  破線 --- → 471 個  1 秒 = --- mm

**色で見分けているので、`rollbook.svg()` の色を変えると壊れる。**
生成 → 読み直しの往復テストが `RollBook` の数え上げと突き合わせているので、
変えれば落ちる（色を 1 だけずらして落ちることを確認済み）。

### S. 新しく足したテンプレートは live reload で拾われない（対応しない）

`tornado.autoreload.watch()` は起動時にあるファイルしか見ないので、
テンプレートや静的ファイルを**新規に足したとき**は手で再起動が要る。
既存ファイルを直したぶんは今までどおり自動で反映される。

開発時だけの話で、ファイルを足すのは稀なので**このままとする**。
（CLAUDE.md の live reload の節に、この制約は書いてある。）

### P. 確認ダイアログの出し方（方針を決めた。コードは変更なし）

**Yes / No は `confirm()`、三択以上は `<dialog>`** という方針にした。
`confirm()` は 2 択にしかならないので、選択肢が増えたら `<dialog>` にする。
逆に、ただの確認に `<dialog>` を持ち出さない。

棚卸ししたところ、**現状すべてこの方針どおりだった**ので、コードは変えていない。

| 場面 | 出し方 |
|---|---|
| 履歴の削除（個別 / 全部） | `confirm()` |
| 機種の削除 | `confirm()` |
| 同名アップロード（3 択） | `<dialog>` |
| 機種の追加 | `<dialog>`（入力を伴うので確認ではない） |

CLAUDE.md の「モーダルは `<dialog>`」という書き方が方針と食い違って見えて
いたので、そこを書き直した（`<dialog>` で確認するときは ESC や ✕ も
キャンセル扱いにすること、も併せて明記）。

### N. 設定エディタが未知のキーを黙って落とす（対応しない）

**設定画面からは未知のキーが生じない。** `gatherFormData()` は `FIELDS` に
並べた既知のキーと `notes` だけで payload を組み立て、`update_model()` は
それで機種のエントリを丸ごと置き換える。画面が知らないキーを作る経路は無い。

残る経路は `~/etc/storgan-conf.json` を手で編集して足した場合だけだが、
**手で編集したときのことは考えない**という判断。よって対応しない。

（`bridge interval` が消えたのは、過去のバージョンが書いたキーが残って
いたため。それは A-1 で廃止済みなので、いま実在するキーはすべて既知。）

### M. base テンプレートを切る

`webroot/templates/base.html` を作り、3 本のページをそこから継承させた。
`<head>`・appbar・フッター・live reload の 1 行が 1 か所になった。

各ページが持つのは差分だけ。

| ブロック | 中身 |
|---|---|
| `head` | ページごとの `<script>`（`MODELS_DATA` など） |
| `content` | `<main>` の中身 |
| `dialogs` | `<main>` の外に置くモーダル（同名確認・機種追加） |
| `scripts` | ページごとの `<script src=…>` |

- ナビの現在地は各ハンドラが `nav='top' / 'history' / 'config'` を渡す。
  テンプレート側で判定すると、結局ページごとに書くことになるため
- `window.URL_PREFIX` は全ページで要るので `base.html` に置いた

Tornado の継承は `{% extends %}` + `{% block %}`。子テンプレートの
ブロック外に書いたものは出力されない（実際に確かめた）。

テスト 4 本追加（178 → 182）。ナビが全ページに出ること、現在地の印が
1 か所だけで正しいリンクに付くこと、CSS と `URL_PREFIX` が全ページに
あること。live reload のテストも履歴を含めた全ページを見るようにした。

**注意**: テンプレートは行頭の空白を落とすので、生成された HTML を
文字列で突き合わせるテストはインデントに依存させないこと（一度これで
落とした）。

### L. 日本語のファイル名がダウンロードできない

`Download.get()` が `'attachment; filename=' + name` と組み立てていたため、
**非 ASCII の名前だと 500 になっていた**（HTTP ヘッダは latin-1 しか通らず、
tornado が弾く）。空白入りは 200 で返るが引用符が無く、ブラウザが名前を
途中で切りうる状態だった。

K で履歴に全ファイルのダウンロードリンクが並び、日本語名の MIDI を
上げていると押すたびに 500 になるので、先に直した。

`storage.content_disposition()` に切り出し、RFC 6266 の形にした。

    attachment; filename="download.mid"; filename*=UTF-8''%E3%83%86%E3%82%B9%E3%83%88%E6%9B%B2.mid

- UTF-8 のままの名前は `filename*` に percent-encode して入れる
  （今のブラウザはこちらを優先する）
- 読まないもの向けに ASCII へ落とした `filename` を引用符付きで併記する。
  NFKD で分解してから ASCII に落とすので `naïve` → `naive`。
  日本語のように全部消える場合は `download` + 拡張子にする
- 引用符・バックスラッシュ・制御文字は `_` にする（quoted-string が壊れるため）

テスト 8 本追加（168 → 178）。組み立てそのものと、実際に 200 で落とせて
中身が一致すること。**直す前のコードに戻すと落ちることを確認済み。**

### O. `webroot/midi/` と `webroot/svg/` が溜まり続ける（対応しない）

上限や自動削除を入れるか検討したが、**K の履歴の画面から見えて消せるので
それで足りる**と判断した。件数や日数での自動削除は入れない。

### K. 履歴の画面

`/storgan2/history` を足した。`webroot/midi/` と `webroot/svg/` の中身が
画面から見えるようになり、消すのにファイルマネージャを開かなくてよくなった。

- 2 カラム（MIDI / SVG）。名前・サイズ・更新日時を新しい順に
- 個別削除と全部削除（どちらも `confirm()` で確認してから）
- ダウンロード。MIDI 用に `/download/midi/<name>` を足した
  （SVG は生成結果の画面のリンクが元からこの形なので `/download/<name>` のまま）
- MIDI を選んで再生成（画面で選んでいる機種で作り直す）
- SVG を選んで再表示（生成し直さずそのまま出す）

**再表示の諸元は SVG から読めるぶんだけ。** `width` / `height` は
`<svg width="…mm" height="…mm">` にあるので読む。穴の数と `mm_per_sec` は
無いので `---` と出す（演奏時間と現在位置の秒表示も `---`）。
「生成しました」ではなく「履歴から表示」と出し、添えるサイズも
元 MIDI ではなく SVG 自身のものにした。

**ファイル名を外から受け取る経路ができた。** `storage.py` に集めてある。
`safe_name()` が区切り文字と `..` を弾き、`resolve_in()` が解決後も置き場の
中にあることを確かめる（シンボリックリンクで外へ出る場合も弾く）。
削除・ダウンロード・再生成・再表示のすべてがこれを通る。

`Handler1.post()` は `stored_midi` / `stored_svg` を見て分岐する。
生成結果の画面を出すのは 1 か所（`_render()`）のままにしたかったため。

テスト 43 本追加（125 → 168）。名前の検証（`..`・区切り文字・リンクの先が外）、
削除の API、再生成と再表示、ダウンロード、ブラウザからの一連の操作。

base テンプレートは今回も切っていない。ページが 3 本になり `<head>` と
appbar と live reload の 1 行が 3 重になった。**次にページを増やすときは
先に切ったほうがよい。**

### I. ブラウザ側の live reload（開発時のみ）

`webapp --debug` で起動すると、テンプレート / CSS / JS を直したときに
ブラウザが勝手に再読み込みされるようにした。実装は `livereload.py`。

方針は計画どおり。サーバー側にファイル監視のロジックは無く、
**「切断そのものが更新の合図」**になっている。

1. `watch_webroot()` が `templates/` と `static/` を `tornado.autoreload` の
   監視対象に足す（`.py` 以外でもプロセスが再起動する）
2. `LiveReloadHandler` は繋がるだけの WebSocket。何も送らない
3. `static/js/livereload.js` が切断を検出し、繋ぎ直せるようになった時点で
   `location.reload()`

**base.html は切らなかった。** live reload 単体には要らず、テンプレート 2 本の
共通化はそれ自体が別の変更になるため。`<script>` の 1 行は 2 箇所に書いてある
（片方だけ直して食い違う余地が残っている点は計画時の指摘のまま）。

テスト 5 本追加。`--debug` のとき `<script>` が両ページに出て WebSocket が
繋がること、既定では出ずエンドポイントも 404 になること。

実際に動かして、テンプレートと CSS のどちらを直しても
ブラウザが自動で更新されることを確認済み。

### 同名の MIDI を上げ直すと、古いほうが使われていた

`Handler1.post()` が `if not file1_path.exists()` で書き込みを省いていたため、
**送られてきた中身を捨てて `webroot/midi/` にある古いファイルを解析していた。**
MIDI を直して同じ名前で上げ直す、という一番ありそうな使い方で踏む。
エラーにならず、SVG の名前も同じなので、**成功したように見えて前回の結果が返る**。
実測でも 2 回目の SVG が 1 回目とバイト単位で一致した。

「確認してから上書き」にした。選択肢は 3 つ。

| ボタン | 送るもの | サーバーの動き |
|---|---|---|
| 置き換える | `overwrite=1` | 送られた中身で置き換えて作り直す |
| 前回の結果を表示 | `reuse=1` | 置き換えず、前回のファイルから作り直す |
| キャンセル | 送らない | — |

- `storgan.js` が送る前に確認する。既にある名前は
  `window.UPLOADED_NAMES`（`webroot/midi/` の一覧）で判定する
- **`confirm()` では 2 択にしかならない**ので `<dialog>` にした。
  ESC や ✕ で閉じた場合もキャンセルと同じ扱いになるよう、選択を覚えてから
  閉じて `close` イベント 1 か所で処理している
- サーバーは `overwrite` も `reuse` も無い同名の POST を断る（古いほうを
  黙って使わない）。画面は先に訊くのでここへは来ないが、経路として塞いでおく
- `reuse` のとき、送られてきた中身は届いているが**書かずに捨てる**。
  ファイル名を別に受け取る形にすると、そこからパスを組み立てることになるので
  避けた（ローカルの道具とはいえ、辿れる余地を増やさない）
- 前回のファイルを使った場合は、結果の画面にその旨を出す（`reused`）

J の「読めなかったファイルを消す」は残してある。消さないと、次に同じ名前で
正しいファイルを送るたびに「既にあります」と言われることになるため。

### J. アップロードの失敗がユーザーに伝わらない

F で見つけた 2 件。どちらも**画面ごと失われて、利用者は選び直すこともできず
ブラウザの戻るしかない**状態だった。

- MIDI でないファイル / 壊れたファイル → 素の 500 ページ
- サイズ上限超え → 真っ白なページ

直し方:

1. `Handler1.post()` の `parse_to_file()` を `try` で囲み、失敗したら
   `_render(msg=..., msg_error=True)` でファイル選択の画面に戻す。
   `msg` は元から `.drop__status` に出る経路があったので、そこに理由を出す。
   `msg_error` は表示を赤くするためだけのフラグ（`.drop__status--error`）
2. 上限超えはハンドラまで来ない（tornado が `max_buffer_size` で本文を
   読まずに接続を切る）ので、**送る前に JS で止める**。テンプレートに
   `size_limit_bytes`（丸めていない素のバイト数）を足し、`storgan.js` が
   `input.files[0].size` と比べる。`<input>` の `onchange` 直書きはやめ、
   送信も JS が受け持つようにした

**併せて、読み込めなかったファイルを残さないようにした。** `post()` は
`if not file1_path.exists()` で書き込みを省くので、壊れたファイルが
`webroot/midi/` に残ると、次に同じ名前で正しいファイルを送っても
壊れたほうが使われる。失敗時に消すようにして、回帰テストも足した
（`unlink` を外すと落ちることを確認済み）。

ブラウザテストは現状固定から**あるべき挙動の確認**に書き換えた（22 本 → 23 本）。

### F. ブラウザテストを整備する

12 本 → 22 本。未着手として挙げていた領域をすべて埋めた。

- 機種の追加を**確定する**ところ（コピー元の中身を引き継いでいること）と、
  名前が空 / 重複のときにサーバーへ送らず断ること
- 機種の削除。`confirm()` は `page.once('dialog', ...)` で捌く。
  キャンセルした場合に**何も起きない**ことも確認する
  （リクエストが飛んでいないかを見る。「起きない」を待てないため）
- 入力値の検証とエラー表示: 機種名が空（送信前に断る）、必須の数値欄が空
  （サーバーの 400 が `showAlert` にそのまま出る。ボタンが
  「保存中...」で固まらないことも）
- アップロード: MIDI でないファイル、サイズ上限超え → **どちらも不具合を
  発見した（J で修正済み）**

土台の追加:

- `restore_conf` fixture — `live_server` はセッション全体で 1 個なので、
  機種を足したり消したりするテストは終わったら設定ファイルを書き戻す。
  ハンドラはリクエストごとに `Conf()` を作り直すため、これでサーバー側も戻る
- `small_limit_server` fixture — 上限超えを試すためだけに、上限 4 KB の
  サーバーを別に立てる（既定は 100 MB で、超えさせるには 100 MB 送ることになる）。
  `Conf.SEARCH_PATH` はクラス変数なので、`live_server` に依存させて
  隔離済みの設定に相乗りさせている
- サーバー起動は `_start_server()` に切り出した（2 つの fixture で共有）

上限超えのテストは、上限を外すと落ちることを確認済み（空振りしていない）。

当初「CI で回すなら Chromium の取得をどうするか」を挙げていたが、
**このプロジェクトでは CI を使わない**方針なので、項目ごと落とした。

### A-2. `note name` / `note offset` を `notes` に統合（A 完了）

2 本の並行配列（`'note name'` と `'note offset'`）を、
`'notes': [{"name": ..., "offset": ...}, ...]` の 1 本にした。
**長さがずれ得る形をやめたので、`validate_config()` の長さ一致チェックが
そもそも不要になった**（代わりに要素ごとに index 付きで検証する）。
`note2scale()` も `NoteConf` のリストを受け取る。

`'name'` の位置づけ（当初の A-2 の宿題）は、統合そのもので解決した。
オフセットと同じ要素の中にあるので「どのトラックの音名か」が構造から自明で、
CLAUDE.md には**表示専用**（SVG 生成は `'offset'` しか見ない）と明記した。

**旧形式はもう読めない**（手動移行）。`~/etc/storgan-conf.json` の 4 機種は
変換済みで、旧ファイルは `storgan-conf.json.old-format-20260804-043049` に退避。
移行の検証として、変更前のコードを worktree に展開し
4 機種 × 5 曲 = 20 通りの SVG を比較 — 全てバイト単位で一致した。

併せて、基準ノート番号（`base note`）を設定エディタの「基本寸法」から
「音階マッピング」へ移した。各トラックの `offset` はこの番号からの半音数なので、
mm の寸法と並べるより表の真上にある方が読める。

なお、直前のコミットの `storgan.conf-dist` → `conf/storgan-conf.json` リネームで
`tests/conftest.py` の参照先が失われ、テストが全 81 件エラーになっていた。
`Conf.CONF_FNAME` を使うようにして解消（`tests/browser/conftest.py` も同様）。

### H. 生成した SVG をブラウザ上でズーム・スクロールできるように

`.svgbox` を倍率つきのビューアにした。ツールバー（倍率スライダ / ± /
高さ合わせ / 全体 / 原寸 / 先頭へ）、位置と演奏時間の読み取り、ミニマップの帯、
Ctrl + ホイールでの拡縮、ドラッグでのパンまで。実装は `storgan.js` の後半。

**transform では拡縮していない。SVG の描画サイズそのものを変えている。**
こうするとブラウザ標準のスクロールがそのまま効き、スクロールバーが全体の中の
現在位置を示す。SVG が mm 単位なので倍率 1.0 がそのまま原寸（96dpi 換算）。
汎用の panzoom ライブラリは transform ベースで、縦横比 33:1 のロールブックでは
スクロールバーが消えて現在位置を見失うので使わなかった。

- 既定は「高さ合わせ」（`d-kaeru.mid` / 34notes で 75%）。「全体」は 7% に
  なって何も読めないので既定にしない
- **初期表示は右端。** `viewBox` が負で、曲の先頭が x=0 側 = 右端にあるため
- `1sec` = 50mm なので、スクロール位置がそのまま演奏時間になる

`RollBook` に `width` / `height` / `hole_count` / `mm_per_sec` を足した。
`Handler1._render()` が `book` として別に渡し、テンプレートが
`window.BOOK_DATA` に出している。

（**追記**: このとき「寸法は SVG からは取り出せない」と書いたが、
`width` / `height` は `<svg width="…mm" height="…mm">` に出ている。
取り出せないのは穴の数と `mm_per_sec` のほう。）

`Handler1` は `post()` が `get(svg_data=...)` を呼ぶ形をやめて、描画を
`_render()` に分けた。GET ハンドラを描画関数として使い回すのは分かりにくく、
`svg_data` が「画面の切り替えフラグ」を兼ねているのも読みにくかった。

ビューアのブラウザテストを 2 本追加（初期位置が右端であること、倍率ボタン）。
倍率の絶対値ではなく「横スクロールが消えるか」で判定しているので、
ウィンドウ幅に依存しない。

### G. Web UI を Pico.css で作り直す

CDN 6 本（Bootstrap CSS/JS、Font Awesome、jQuery、popper、socket.io）を全廃した。
**ローカルで動かす道具なのに、ネットに繋がっていないとレイアウトが崩れていた。**
Pico.css v2.1.1 を `webroot/static/css/pico.min.css` に同梱し、アイコンは
インライン SVG、フォントはシステムフォントに統一した。

モック（3 画面、実データ入り、明暗両テーマ）:
<https://claude.ai/code/artifact/05378ca3-d845-4e9d-8b95-6e591105684e>

- 配色は生成される SVG から採った（外枠の青 `#0000FF` → 主要アクション `#2947c8`、
  穴の赤 `#FF0000` → 破壊的操作 `#c8392f`）。数値は等幅 + `tabular-nums`
- `config_editor.js` は jQuery をやめて素の DOM API に書き直した。文字列連結で
  HTML を組み立てていた箇所も `createElement` にしたので、値の中の引用符で
  壊れることがなくなった。モーダルは Bootstrap から `<dialog>` へ
- `storgan.js` を新設（機種を選ぶと寸法サマリを出す。生成してから
  「機種が違った」と気づくのを避けるため）。空だった `my.js` は削除

作業中に判明したこと（`CLAUDE.md` にも書いた）:

- **`:root` で Pico の変数を上書きしても効かない。** Pico は
  `:root:not([data-theme=dark])`（詳細度 (0,2,0)）で書いているので、あとから
  読み込んでも負ける。`:root:root` にして解決した
- **テンプレートは `autoreload=True` でも再読み込みされない**
  （`compiled_template_cache`）。直したらサーバーの再起動が要る
- 静的ファイルは `{{ static_url(...) }}` に変えた。`?v=<hash>` が付くので、
  CSS を直したのに古いキャッシュを掴む、という事故が起きない

テスト: `test_upload_midi_renders_svg_preview` の `page.locator('svg')` だけ
`#svgbox svg` に変えた（ロゴがインライン SVG になり、複数該当するため）。
その他はすべて要素 ID で操作しているのでそのまま通った。

### B. `os.path` → `pathlib` 移行

26 件すべて解消。`per-file-ignores` の移行チェックリストは空になった。

B-1（`webroot` / `workdir` の `str` 配線）も同時に解消。`WebServer` が
`Path` に正規化し、`app.settings` にも `Path` のまま渡すようにしたので、
各ハンドラは `self._webroot / 'svg' / fname` と書ける。

`Conf.SEARCH_PATH` の `Path('.')` だけは `PTH201` を除外して残した
（探索対象がカレントであることを明示するほうが読みやすいため）。

検証: SVG 出力が HEAD とバイト一致（195,330 bytes）。CLI・Web とも通しで動作確認。

### `webroot/svg/` の古い成果物を削除

`127b94d`（hairline 対応）より前に生成された 7 件を削除した。`stroke-width` も
book height も現行と異なり、出力を目視比較するときに紛らわしかった。
元 MIDI は `webroot/midi/` に残っているのでいつでも再生成できる。

### Claude Code のプラグインをこのプロジェクトで無効化

`github` / `frontend-design` は 60 起動で 0 回だった。他プロジェクトでは使うため、
ユーザースコープ（`~/.claude/settings.json`）は有効のまま、このプロジェクトの
`.claude/settings.local.json` で `false` にした（設定の優先順位は
ユーザー < プロジェクト < ローカル）。`pyright-lsp` は Python なので有効のまま。
※ `settings.local.json` は gitignore されているため、リポジトリには残らない。

### `URL_PREFIX_HANDLER1` を削除

`/{prefix}/handler1.*` のルートは、テンプレートからも JS からも参照されない
死んだ経路だった。冗長だった `url_prefix_handler1` 設定も外し、`handler1.py` の
`_url_path` は `_urlprefix` から組み立てるようにした。
末尾スラッシュなしのリダイレクト（`/px` → 301 → `/px/`）は従来どおり。

### E. URL prefix の扱いを整理

テストの `/storgan2` 直書き 12 箇所を排除し、テスト全体を既定値以外の
prefix（`/storgan-test`）で動かすようにした。テンプレートや JS が prefix を
直書きすると `test_static_assets_load` が落ちる（実際に壊して検証済み）。

**併せて発覚した問題を修正**: `test_config_handler` の add/delete テストが
`~/etc/storgan-conf.json`（利用者の実設定）を書き換えていた。
`tests/conftest.py` の autouse fixture で `Conf.SEARCH_PATH` を一時ディレクトリに
差し替え、全テストから実設定を隔離した。

### `archives/` を追跡対象に

過去の計画書 4 件。最新仕様の参考にはならないが、記録として残す。

### C. README.md を現状に合わせて書き直し

インストール/ダウンロード手順を全削除し、機能の説明を中心にした。
旧リポジトリの clone 手順、廃止された URL・コマンド名、存在しない
画像への参照がすべて消えた。使い方は `--help` への誘導のみ。

### A-3. 数値変換の重複を解消

`update_model()` / `add_model()` に同一の変換ブロックが 2 箇所あり、
さらに `validate_config()` が必須項目一覧を別途持っていた（計 3 箇所）。
`NUMERIC_FIELDS`（項目名 → 変換関数の dict）に集約し、検証と変換の
両方がこれを参照するようにした。**設定項目の増減はここ 1 箇所で済む。**

副次的に、`base note` に `"60.5"` のような値を渡すと検証を通ったあとで
`int()` が `ValueError` を投げて 500 になっていた不具合が解消した
（検証に変換関数そのものを使うようにしたため 400 が返る）。回帰テスト追加済み。

### A-1. `bridge interval` を設定項目から削除

`validate_config()` が必須項目として要求していたが `rollbook.py` は一度も
読んでいなかったため、スキーマ・検証・設定エディタ・設定テンプレート・テストの
全てから削除した。穴の分割に使われるのは `bridge_width` と `bridge_threshold` のみ。

既存の設定ファイルにキーが残っていても検証は通る（未知のキーは素通りする）。

**追記**: `~/etc/storgan-conf.json` に未使用キーとして残っていた分も削除し、
`bridge interval` は完全に廃止した。値はどちらも 10.0 だった
（退避: `storgan-conf.json.bridge-interval-20260804-171353`）。
なお設定エディタは既知の項目だけを組み立てて送るので、**未知のキーは
一度保存すると消える**。4 機種のうち 2 つはそれで先に消えていた。

### `82aaa65`

- テスト 2 件の失敗を解消（`validate_config` の型ガード追加、古い期待値の更新）
- ruff の設定整備（`select` 拡張、`line-length = 88`、指摘 76 件を解消）
- flake8 を削除（無設定のまま ruff と併存し、8 対 102 件で食い違っていた）
- `RollBookApp` の `-o` が無視されていた問題を修正
- `uv.lock` を追跡対象に
- `CLAUDE.md` を追加
