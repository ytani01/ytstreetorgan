# TODO-046. `ytmidilib` をタグで固定する

要求書（TODO-045）に「バージョンを固定できない」として書いた項目は、
**こちら側だけで片付いた**。上流にはすでにタグが打たれていた
（`0.0.1` / `0.0.2` / `0.0.3`。`v` は付かない）。

`0.0.3` は当時の既定ブランチの先頭（`4bd49e8`）と同じで、`uv.lock` が
掴んでいるコミットとも一致したので、**固定しても取り込む中身は
変わらなかった**。

- [x] `pyproject.toml` の `[tool.uv.sources]` に `tag = "0.0.3"` を足す
- [x] `uv lock` で lock を更新（リビジョンは変わらないことを確認）
- [x] 要求書から当該項目（14 番）を削除。
      「4・7 はそのまま入れてよい」と互換性の方針に書き足した

固定したので、上流が挙動を変える修正（要求の 4・7）を入れても不意打ちに
ならず、上げるタイミングをこちらで選べるようになった。実際に上げたのは
TODO-047（`0.1.0`）。
