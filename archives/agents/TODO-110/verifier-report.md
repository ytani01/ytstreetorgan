# TODO-110 確認担当報告

## 検証実行

### テスト結果
- `uv run pytest -q` — 303 passed, 49 deselected（終了コード 0）
- `uv run ruff check src tests` — All checks passed（終了コード 0）
- `uv run mypy src` — Success: no issues found（終了コード 0）
- `uv run python docs/images/make_shots.py` — スクリーンショット再生成、正常終了

### 画像の確認

1. **web-upload.png**: 確認済み。3 つの案内が画像に写っている
2. **web-result.png / web-transpose.png / web-history.png / web-config.png**: 戻された 4 枚も確認。現在の画面と一致（タイムスタンプ以外）

## 見つかった問題

### 1. 画面との wording 不一致（要修正）

**場所**: docs/User.md 3.2 の 2 番目の箇条書き（73 行目）

**実装内容**:
```
- 生成後、移調の候補の一覧を表示する
```

**画面実際の表現** (`webroot/templates/storgan.html` 行 31):
```html
<li>生成後、候補の一覧を表示します。</li>
```

**問題**: 実装報告で「画面の表現に揃え」と書かれているが、画面は「候補の一覧」で、docs は「**移調の**候補の一覧」と 「移調の」を追加している。画面には無い言葉が加わっている。

### 2. 表現の一貫性不足

- 3.2 の 2 番目の箇条書き（73 行）: 「生成後、**移調の**候補の一覧を表示する」
- 3.2 の手順後（82 行）: 「作ると**候補の表**が出る」（「移調の」が無い）
- 3.4 本文（145 行）: 「その**候補が表**になっている」（「移調の」が無い）

同じものを指しているのに、3.2 の箇条書きだけ「移調の」が付いている。3.4 のタイトルが「移調の候補から選び直す」なので、そこで初めて「移調の」候補だと明らかになる構成になっている。

### 3. その他の wording 差異（軽微）

画面と docs の wording は以下で異なるが、意味は同じ:

| 項目 | 画面 | docs |
|---|---|---|
| 1 番目 | 穴が開きません**が** | 穴が開かない**が** |
| 1 番目 | 最適化します。 | 最適化する |
| 3 番目 | 残ります。 | 残る。 |
| 3 番目 | 削除できます。 | 削除できる |

これらは助動詞や句読点の違い（である調）で、意味内容は変わらない。

## 確認済み事項

✓ 画像 5 枚が現在の画面と一致（タイムスタンプ除く）
✓ テスト 3 種類（pytest / ruff / mypy）すべて通過
✓ section 3 全体が基本的に実画面と合致（上述の wording 差異除く）
✓ 「造語」「裸の『ノート』」などの禁止表現はなし
✓ 「調」「オクターブ」の用語が画面向け文書に出ていない（正しく外されている）

## 判断が必要な点

**docs 3.2 の 2 番目の箇条書きをどうするか**:

1. **案 A**: 「生成後、候補の一覧を表示する」に直す（画面に合わせる）
   - 利点: 画面と一字一句同じになる
   - 懸念: 「候補」だけだと読者によっては何の候補か不明確かもしれない

2. **案 B**: 「生成後、移調の候補の一覧を表示する」のまま（現状）
   - 利点: より具体的で分かりやすい
   - 懸念: 画面表記と食い違う、実装報告の「画面の表現に揃え」という述べと矛盾

3. **案 C**: 条件付きで受け入れる: 「実装報告で意図的な改変であることが確認できるなら OK」としてもよいが、報告には明記されていない
